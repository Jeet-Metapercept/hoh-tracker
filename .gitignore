# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Standalone bot (kept out of the frontend repo — has its own secrets).
# Anchored to root so it does NOT match server/api/atlantis/.
/atlantis/
